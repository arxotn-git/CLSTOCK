name: 위킵 재고 자동 동기화

# 실행 조건: 매일 자동 실행 + 언제든 수동으로도 실행 가능
on:
  schedule:
    # 매일 한국시간(KST) 새벽 3시에 실행 (UTC 기준 18시 = KST 다음날 03시)
    - cron: "0 18 * * *"
  workflow_dispatch: {}   # GitHub 화면에서 "Run workflow" 버튼으로 수동 실행 가능

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 저장소 코드 가져오기
        uses: actions/checkout@v4

      - name: 파이썬 설치
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 필요한 패키지 설치
        run: |
          pip install -r requirements.txt
          playwright install --with-deps chromium

      - name: 위킵 재고 동기화 스크립트 실행
        env:
          WEEKEEP_ID: ${{ secrets.WEEKEEP_ID }}
          WEEKEEP_PW: ${{ secrets.WEEKEEP_PW }}
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
        run: python weekeep_sync.py

      - name: 실패 시 화면 캡처 첨부
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshot
          path: debug/
          retention-days: 7
